name: firebase_chat_reply_example
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0

  # firebase
  firebase_core: ^0.5.0+1
  cloud_firestore: ^0.14.1+3

  swipe_to: ^0.0.1+6

  foundation:
    path: foundation/

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true